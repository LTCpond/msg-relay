<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ltcpond.msgrelay.user.repository.LoginDeviceMapper">

    <resultMap id="BaseResultMap" type="com.ltcpond.msgrelay.user.model.entity.LoginDevice">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="device_id" property="deviceId"/>
        <result column="device_type" property="deviceType"/>
        <result column="ip" property="ip"/>
        <result column="refresh_token" property="refreshToken"/>
        <result column="last_active_at" property="lastActiveAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">
        id, user_id, device_id, device_type, ip, refresh_token, last_active_at, created_at, updated_at, deleted
    </sql>

    <insert id="insert" parameterType="com.ltcpond.msgrelay.user.model.entity.LoginDevice">
        INSERT INTO t_login_device (id, user_id, device_id, device_type, ip, refresh_token, last_active_at, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{deviceId}, #{deviceType}, #{ip}, #{refreshToken}, #{lastActiveAt}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.ltcpond.msgrelay.user.model.entity.LoginDevice">
        UPDATE t_login_device SET refresh_token = #{refreshToken}, last_active_at = #{lastActiveAt}, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/> FROM t_login_device WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/> FROM t_login_device WHERE user_id = #{userId} AND deleted = 0 ORDER BY last_active_at DESC
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM t_login_device WHERE user_id = #{userId} AND deleted = 0
    </select>

    <update id="deleteByIdLogic">
        UPDATE t_login_device SET deleted = 1, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="deleteByUserIdAndDeviceId">
        UPDATE t_login_device SET deleted = 1, updated_at = NOW() WHERE user_id = #{userId} AND device_id = #{deviceId} AND deleted = 0
    </update>

    <select id="selectByUserIdAndDeviceId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/> FROM t_login_device
        WHERE user_id = #{userId} AND device_id = #{deviceId} AND deleted = 0
    </select>

    <select id="existsByUserIdAndDeviceId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM t_login_device
        WHERE user_id = #{userId} AND device_id = #{deviceId} AND deleted = 0
    </select>

</mapper>
