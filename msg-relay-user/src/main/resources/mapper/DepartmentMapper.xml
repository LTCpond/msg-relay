<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ltcpond.msgrelay.user.repository.DepartmentMapper">

    <resultMap id="BaseResultMap" type="com.ltcpond.msgrelay.user.model.entity.Department">
        <id column="id" property="id"/>
        <result column="team_id" property="teamId"/>
        <result column="name" property="name"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">id, team_id, name, sort_order, created_at, updated_at, deleted</sql>

    <insert id="insert" parameterType="com.ltcpond.msgrelay.user.model.entity.Department">
        INSERT INTO t_department (id, team_id, name, sort_order, created_at, updated_at)
        VALUES (#{id}, #{teamId}, #{name}, #{sortOrder}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.ltcpond.msgrelay.user.model.entity.Department">
        UPDATE t_department SET name = #{name}, sort_order = #{sortOrder}, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/> FROM t_department WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByTeamId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/> FROM t_department
        WHERE team_id = #{teamId} AND deleted = 0 ORDER BY sort_order ASC
    </select>

    <update id="deleteByIdLogic">
        UPDATE t_department SET deleted = 1, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>
