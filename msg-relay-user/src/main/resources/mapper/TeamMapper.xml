<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ltcpond.msgrelay.user.repository.TeamMapper">

    <resultMap id="BaseResultMap" type="com.ltcpond.msgrelay.user.model.entity.Team">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="logo" property="logo"/>
        <result column="owner_id" property="ownerId"/>
        <result column="max_members" property="maxMembers"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">id, name, logo, owner_id, max_members, created_at, updated_at, deleted</sql>

    <insert id="insert" parameterType="com.ltcpond.msgrelay.user.model.entity.Team">
        INSERT INTO t_team (id, name, logo, owner_id, max_members, created_at, updated_at)
        VALUES (#{id}, #{name}, #{logo}, #{ownerId}, #{maxMembers}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.ltcpond.msgrelay.user.model.entity.Team">
        UPDATE t_team SET name = #{name}, logo = #{logo}, max_members = #{maxMembers}, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/> FROM t_team WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/> FROM t_team WHERE deleted = 0
    </select>

    <update id="deleteByIdLogic">
        UPDATE t_team SET deleted = 1, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>
