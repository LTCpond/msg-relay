-- ============================================================
-- 迁移脚本: 用户表改造 + 团队管理员
-- ============================================================

USE msg_relay;

-- 01-schema.sql 已是最终结构。此文件保留编号仅为兼容既有初始化顺序，
-- 避免新建数据库时重复 ADD COLUMN / ADD INDEX 导致初始化中断。
-- 旧库升级脚本统一放在 docker/mysql/migrations，需备份后按编号手工执行。
